# -*- coding: utf-8 -*-
from enum import Enum


class Components(Enum):

    DISPLAY = "Display"

    NEXT_PATCH = "Next patch button"
    BEFORE_PATCH = "Before patch button"

    EFFECT = "Effect button"

    ROTARY_ENCODER = "Rotary encoder with button"
